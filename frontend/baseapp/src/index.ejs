<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pragmatic-dev</title>

    <!--
      Shared design tokens. CSS custom properties PIERCE the Shadow DOM boundary,
      so the tips/query parcels (which isolate their Tailwind inside shadow roots)
      can still consume these for a consistent theme.
    -->
    <style>
      :root {
        --brand: #4f46e5;
        --brand-dark: #4338ca;
        --panel-width: 30%;
      }
      html, body { margin: 0; height: 100%; }
      /* single-spa-layout renders the app layout (a full-height flex row) directly
         into <body>, so we don't need a separate mount element. */
    </style>

    <meta name="importmap-type" content="systemjs-importmap" />

    <!-- Import map: how SystemJS resolves each module.
         Local dev → the parcels' dev servers (:9001/:9002).
         Prod (CDN) → same-origin paths, since all bundles are served from the
         same CloudFront distribution as this index.html. -->
    <script type="systemjs-importmap">
      {
        "imports": {
          "single-spa": "https://cdn.jsdelivr.net/npm/single-spa@6.0.1/lib/es5/system/single-spa.min.js",
          "react": "https://cdn.jsdelivr.net/npm/react@18.2.0/umd/react.production.min.js",
          "react-dom": "https://cdn.jsdelivr.net/npm/react-dom@18.2.0/umd/react-dom.production.min.js",
          "@pragmatic-dev/root-config": "<% if (isLocal) { %>//localhost:9000/pragmatic-dev-root-config.js<% } else { %>/pragmatic-dev-root-config.js<% } %>",
          "@pragmatic-dev/tipsapp": "<% if (isLocal) { %>//localhost:9001/pragmatic-dev-tipsapp.js<% } else { %>/pragmatic-dev-tipsapp.js<% } %>",
          "@pragmatic-dev/queryapp": "<% if (isLocal) { %>//localhost:9002/pragmatic-dev-queryapp.js<% } else { %>/pragmatic-dev-queryapp.js<% } %>"
        }
      }
    </script>

    <% if (!isLocal) { %>
    <!-- Prod API base: the MFEs read these globals (falling back to
         localhost:8000 in dev). In prod the backend is reached at the `api.`
         subdomain via GoDaddy CNAME → CloudFront → EC2 (nginx → FastAPI). -->
    <script>
      window.__TIPS_API_BASE__ = "https://api.pragmatic-dev.in";
      window.__QUERY_API_BASE__ = "https://api.pragmatic-dev.in";
    </script>
    <% } %>

    <!-- Lets you override import-map entries at runtime (dev tooling). -->
    <script src="https://cdn.jsdelivr.net/npm/import-map-overrides@3.1.1/dist/import-map-overrides.js"></script>

    <!-- SystemJS module loader + extras to consume UMD (react) bundles. -->
    <script src="https://cdn.jsdelivr.net/npm/systemjs@6.15.1/dist/system.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/systemjs@6.15.1/dist/extras/amd.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/systemjs@6.15.1/dist/extras/named-exports.min.js"></script>
  </head>
  <body>
    <!-- single-spa-layout builds the applications' layout and appends it to
         <body> at runtime (root-config imports the layout as a string). -->

    <script>
      System.import("@pragmatic-dev/root-config");
    </script>

    <import-map-overrides-full
      show-when-local-storage="devtools"
      dev-libs
    ></import-map-overrides-full>
  </body>
</html>

